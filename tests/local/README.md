# Local Tests (Layer 1)

Fast checks that run on the agent's VM before committing.
Examples: unit tests, docker build, lint, dry-run validation.

Place executable test scripts here. They should exit 0 on success, non-zero on failure.
