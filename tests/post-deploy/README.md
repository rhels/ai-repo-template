# Post-Deploy Tests (Layer 3)

Tests that verify a change after it has been merged and deployed.
Examples: Argo CD sync status, production health checks.

Place executable test scripts here. They should exit 0 on success, non-zero on failure.
